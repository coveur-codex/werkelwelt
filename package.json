{
  "name": "werkelwelt",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript-Monorepo fuer die Werkelwelt PWA/Web-App.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"No development services configured yet.\"",
    "build": "echo \"No build targets configured yet.\"",
    "test": "echo \"No tests configured yet.\""
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
